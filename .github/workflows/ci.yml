name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    uses: fissible/.github/.github/workflows/test-bash.yml@main
    with:
      bootstrap-command: pip3 install pytest
